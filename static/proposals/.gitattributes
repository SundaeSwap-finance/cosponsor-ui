# hash-locked CIP-108 anchor metadata: keep LF so the served bytes match the on-chain dataHash
*.jsonld text eol=lf
# hash-locked constitution document (NewConstitution constitutionAnchor)
*.txt text eol=lf
