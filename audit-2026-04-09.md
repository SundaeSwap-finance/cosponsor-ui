# Codebase Audit Report - 2026-04-09

## Summary
- **Project**: cosponsor-ui
- **Stack**: React 19, Vite 7, TypeScript, Tailwind CSS 4, Bun
- **Scope**: Entire codebase (src/)
- **Files analyzed**: 89
- **Issues found**: 27 (6 critical, 15 warnings, 6 info)
- **Resolved**: 25 | **Deferred**: 2

## Auto-Fixed Issues
- [x] Renamed `src/devData/Proposal Mock Data.tsx` to `ProposalMockData.tsx` (space in filename)
- [x] Renamed `src/components/modals/searchProposals/SearchResultProposal..tsx` to `SearchResultProposal.tsx` (double-dot)
- [x] Fixed import path in `ModalSearchProposals.tsx` to match renamed file
- [x] Fixed missing `setTimeout` cleanup in `ProgressMilestones.tsx` useEffect (memory leak)
- [x] Renamed `src/types/ProposalFilters.ts` to `IProposalFilters.ts` to match import paths

## Critical Issues

### Architecture
- [x] `ModalSponsor.tsx` (741 -> 470 lines) — extracted governance actions, cardanoscan URLs, transaction signing to `src/lib/cardano/` modules (Phases A+C)
- [x] `ModalWithdraw.tsx` (429 -> 350 lines) — extracted transaction signing to shared `signAndSubmitTransaction()` (Phase C)
- [x] Missing `src/lib/cardano/` directory — created with `governanceActions.ts`, `cardanoscan.ts`, `transactionSigner.ts`

### TypeScript
- [x] `as any` on CBOR deserialization — replaced with `Core.HexBlob(witnessSetHex)` to properly brand the hex string. Now centralized in `transactionSigner.ts` only.

### React
- [x] Business logic in presentation components — governance action building extracted to `governanceActions.ts`, transaction signing to `transactionSigner.ts`, wallet verification to `useWalletVerification` hook (Phases A+B+C)
- [x] No error boundaries — added `ErrorBoundary` component wrapping `<Outlet />` in `App.tsx`

## Warnings

### Code Duplication
- [x] Transaction signing/submission — extracted to `src/lib/cardano/transactionSigner.ts` (Phase C)
- [x] Wallet verification — extracted to `src/composables/useWalletVerification.tsx` (Phase B)
- [x] Governance action mappings — extracted to `src/lib/cardano/governanceActions.ts` (Phase A)
- [x] Filter components (6 files) — reviewed, no action needed. Components share a pattern (reset button) but not meaningful code. Each has fundamentally different UI and state shapes; extracting a wrapper would be premature abstraction.

### Console Logging
- [x] 52 console.log/warn statements — migrated to centralized `src/lib/logger.ts` with `debug`/`info`/`warn` levels, gated by `import.meta.env.DEV`. Production builds tree-shake to no-ops.
- [x] ModalWithdraw error inspection block (10 lines) — consolidated into 1 structured `logger.debug()` call
- [x] All `eslint-disable-next-line no-console` directives removed (only `logger.ts` itself has the directive)

### Style Inconsistencies
- [x] Relative imports — fixed in `App.tsx`, `govToolsProposals.ts`, `InputDefault.tsx`, `InputIcon.tsx`, `FilterPropBudget.tsx` to use `@/` alias
- [x] Default export in `govToolsApi.tsx` — converted to named export, updated import site

### TypeScript
- [x] `as unknown as number` setTimeout casts — replaced with `ReturnType<typeof setTimeout>` in `CardProposal.tsx` and `PageProposalDetails.tsx`
- [ ] Route param `as string` assertions in `PageProposalsCategory.tsx:30,48` — useParams returns potentially undefined. Low risk (route guarantees param exists). **Deferred.**

### React
- [ ] Index-based keys in `ProgressMulti.tsx:41` and `ProgressMilestones.tsx:33`, complex JSX in `PageProposalDetails.tsx`, memoization in ModalSponsor — low impact, no user-facing bugs. **Deferred.**

### Other
- [x] Unused function `verifyUtxoOnChain()` — removed along with commented-out debug block that referenced it
- [x] Hardcoded `preview.cardanoscan.io` URLs — replaced with `getExplorerTxUrl()` from `src/lib/cardano/cardanoscan.ts`, derives URL from `COSPONSOR_BLOCKFROST_NETWORK` env var
- [x] Removed dead commented-out `console.warn` in `PageProposalDetails.tsx`

## Security Status
- **No private keys logged** — CIP-30 wallet bridge properly used
- **No XSS vectors** — no dangerouslySetInnerHTML or innerHTML
- **No eval/Function** — no code injection risk
- **.env properly gitignored** — secrets not committed
- **Dev logging gated** — all debug/info/warn calls via `logger.*`, stripped in production builds

## Dependency Status
See `milestones/m2-feedback.md` — full package audit completed separately.

## Files Reviewed
All 89 TypeScript/React files in src/ including components, pages, hooks, composables, API layer, types, and configuration.
