# Codebase Audit Report - 2026-04-09

## Summary
- **Project**: cosponsor-ui
- **Stack**: React 19, Vite 7, TypeScript, Tailwind CSS 4, Bun
- **Scope**: Entire codebase (src/)
- **Files analyzed**: 89
- **Issues found**: 27 (6 critical, 15 warnings, 6 info)
- **Auto-fixed**: 4 issues

## Auto-Fixed Issues
- [x] Renamed `src/devData/Proposal Mock Data.tsx` to `ProposalMockData.tsx` (space in filename)
- [x] Renamed `src/components/modals/searchProposals/SearchResultProposal..tsx` to `SearchResultProposal.tsx` (double-dot)
- [x] Fixed import path in `ModalSearchProposals.tsx` to match renamed file
- [x] Fixed missing `setTimeout` cleanup in `ProgressMilestones.tsx` useEffect (memory leak)
- [x] Renamed `src/types/ProposalFilters.ts` to `IProposalFilters.ts` to match import paths

## Critical Issues

### Architecture
- [ ] `ModalSponsor.tsx` (741 lines) — mixes UI, transaction building, wallet signing, blockchain validation, and error handling. Extract transaction logic to `src/lib/cardano/` hooks.
- [ ] `ModalWithdraw.tsx` (429 lines) — same mixed concerns as ModalSponsor, ~95% duplicated transaction/signing logic between the two.
- [ ] Missing `src/lib/cardano/` directory — CLAUDE.md convention says Cardano logic belongs there, but blockchain code is scattered across modal components and composables.

### TypeScript
- [ ] `as any` type assertions on CBOR deserialization — `ModalSponsor.tsx:504`, `ModalWithdraw.tsx:196` — `Core.TransactionWitnessSet.fromCbor(witnessSetHex as any)`. Create proper type or use type guard.

### React
- [ ] Business logic in presentation components — ModalSponsor contains governance action building (lines 151-268), transaction building (lines 413-528), and wallet signing (lines 485-561) that should be in custom hooks.
- [ ] No error boundaries — async blockchain operations can fail silently (e.g., `useGetProposalData.tsx:188` catches and returns `[]`).

## Warnings

### Code Duplication
- [ ] Transaction signing/submission flow ~95% identical between ModalSponsor (lines 373-574) and ModalWithdraw (lines 124-275). Extract to shared `useTransactionSigner()` hook.
- [ ] Wallet verification logic duplicated in `ButtonSponsor.tsx:25-33` and `ButtonWithdraw.tsx:22-30`. Extract to `useWalletVerification()` hook.
- [ ] Filter components (6 files) all follow identical pattern: useState, update handler, reset button. Consider base component or factory.
- [ ] Governance action category mappings (`CATEGORY_TO_ACTION_KIND`, `SUPPORTED_ACTION_TYPES`, `ACTION_TYPE_DISPLAY_NAMES`) defined in `ModalSponsor.tsx` — should be in shared config module.

### Console Logging
- [ ] 78 console statements across codebase (30+ in ModalSponsor, 25+ in ModalWithdraw, 18 in useGetProposalData). Most have `eslint-disable` comments. Should wrap in `import.meta.env.DEV` or use centralized logger before production.
- [ ] `ModalWithdraw.tsx:243-259` has 15 separate console.log calls for error property inspection — replace with structured error logging.

### Style Inconsistencies
- [ ] 5 files use relative imports instead of `@/` path alias: `App.tsx`, `govToolsApi.tsx`, `InputDefault.tsx`, `InputIcon.tsx`, `FilterPropBudget.tsx`
- [ ] 1 file uses default export (`govToolsApi.tsx:17`) — all other files use named exports.

### TypeScript
- [ ] `as unknown as number` double casts for setTimeout in `CardProposal.tsx:96` and `PageProposalDetails.tsx:112`. Use `ReturnType<typeof setTimeout>` instead.
- [ ] Route param `as string` assertions in `PageProposalsCategory.tsx:30,48` — useParams returns potentially undefined. Add runtime guard.

### React
- [ ] Index-based keys in `ProgressMulti.tsx:41` and `ProgressMilestones.tsx:33` — should use stable IDs.
- [ ] Complex JSX in `PageProposalDetails.tsx` (3-level ternary) and `ModalSponsor.tsx` (nested state conditionals). Extract to sub-components.
- [ ] `buildGovernanceAction()` and `buildCosponsoredProposal()` in ModalSponsor not memoized despite being called on render.

### Other
- [ ] Unused function `verifyUtxoOnChain()` in `ModalSponsor.tsx:35-81` with `eslint-disable` for no-unused-vars. Remove or move to utility module.
- [ ] Hardcoded `preview.cardanoscan.io` URLs in ModalSponsor (3 locations) and ModalWithdraw (1 location). Should use env var for network-dependent explorer URLs.

## Recommendations

### Phase 1: Extract Blockchain Logic (highest impact)
- [ ] Create `src/lib/cardano/transactionBuilder.ts` — shared transaction building/signing/submission
- [ ] Create `src/hooks/useTransactionPreview.ts` — shared fee preview logic
- [ ] Create `src/hooks/useWalletSigning.ts` — shared wallet interaction
- [ ] Move governance action mappings to `src/config/governance.ts`

### Phase 2: Code Quality
- [ ] Create centralized logger with `import.meta.env.DEV` guard
- [ ] Replace `as any` with proper CBOR types
- [ ] Fix all relative imports to use `@/` alias
- [ ] Extract wallet verification to shared hook

### Phase 3: React Patterns
- [ ] Add error boundary component for blockchain operations
- [ ] Memoize expensive computations in modal components
- [ ] Extract complex JSX into focused sub-components

## Security Status
- **No private keys logged** — CIP-30 wallet bridge properly used
- **No XSS vectors** — no dangerouslySetInnerHTML or innerHTML
- **No eval/Function** — no code injection risk
- **.env properly gitignored** — secrets not committed
- **Dev logging present** — wrap in DEV guard before production

## Dependency Status
See `milestones/m2-feedback.md` — full package audit completed separately this session.

## Files Reviewed
All 89 TypeScript/React files in src/ including components, pages, hooks, composables, API layer, types, and configuration.
